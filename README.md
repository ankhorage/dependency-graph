# @ankhorage/dependency-graph

Project-aware dependency analysis that produces canonical `@ankhorage/graph` data.

The first analyzer covers TypeScript static imports. Project detection is delegated to `@ankhorage/project-detector`. Cytoscape, UI, update execution, and GitHub transport are intentionally outside this package.
